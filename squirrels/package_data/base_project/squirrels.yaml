modules: []

project_variables:
  product: sample
  major_version: 1
  minor_version: 0

db_connections:
  default: 
    credential_key: null
    url: 'sqlite://${username}:${password}@/./database/sample_database.db'

datasets:
  sample_dataset:
    label: Sample Dataset
    database_views:
      database_view1: 
        file: database_view1.sql.j2
        db_connection: default
    final_view: database_view1

settings:
  auth.enabled: false

# Default values for settings:
#   auth.enabled: false
#   auth.token.expire.minutes: 30
#   parameters.cache.size: 1024
#   parameters.cache.ttl.seconds: 0
#   results.cache.size: 128
#   results.cache.ttl.seconds: 0
